// FUNCTIONS

// Generate each list item for the tasks

const generateListItems = function () {
  // Set classes for main divs beforehand. Makes it easy to tweak later
  let liClasses =
    "row item align-items-center text-light border-bottom border-1 border-light";
  let timeSlotClasses =
    "fill timeSlot p-2 col-1 text-dark text-end rounded-start";
  let taskSlotClasses = "fill taskSlot p-2 col-10";
  let saveSlotClasses = "fill saveSlot p-2 col-1 fill rounded-end";

  // Create the needed elements for each list item
  let listItemEl = $;
};

let listEl = $(".list");
console.log(listEl);
